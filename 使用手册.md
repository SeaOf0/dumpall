# dumpall 使用手册

## 1. 工具定位

`dumpall` 是只读、离线优先的主机应急响应工具。它采集 Linux/Windows 主机状态、进程、网络、账户、持久化、日志、Web 文件和可选内存证据，然后生成结构化结果、报告和证据包。

它不执行漏洞利用、爆破、主动扫描、隔离、删除或自动修复。输出结果是证据和线索，不是单独的入侵定论。

建议在受控目录运行，并使用管理员/root 权限以获得完整字段。输出目录必须不存在，工具不会覆盖已有目录。

## 2. 最常用命令

### 2.1 快速扫描

```bash
./dumpall scan --output ./results
```

默认分析最近 3 天（72 小时）的 Web/应用输入，输出基础主机信息、进程、网络、账户、持久化和文件线索。

`scan` 和 `triage` 都会先执行同一套基础快照，基础结果一定落在
`collection/`（表格使用 CSV，进程树使用 TXT，系统摘要使用 JSON）：

| 基础类别 | Linux / Windows 输出 | 采集内容 |
|---|---|---|
| 系统 | `collection/system_info.json` | 主机名、操作系统/版本、内核、架构、启动时间、时区、采集权限和运行环境 |
| 进程 | `processes.csv`、`process_tree.txt` | PID/PPID、名称、可执行路径、完整命令行、启动时间、用户、父子关系、Web 相关标记 |
| 网络 | `network_connections.csv` | TCP/UDP 监听和连接、本地/远端地址端口、状态、PID/进程名、远端地址分类 |
| 账户 | `users.csv`、`privileged_users.csv`、`logons.csv` | 本地/域用户、启用状态、UID/SID、管理员或特权组、当前会话 |
| 隐藏/可疑账户 | Linux `account_security.csv`；Windows `users_hidden_check.csv` | UID 0 复用、服务账户登录 Shell、重复 UID、空密码/弱哈希（可读时）；Windows SAM 名称与正常枚举差集、`$` 后缀账户 |
| 持久化 | `scheduled_tasks.csv`、`startup_items.csv`、`services.csv` | cron/systemd/计划任务、启动项、服务及其命令/路径/账户 |
| 文件线索 | `web_roots.csv`、`discovered_*_logs.csv`、`recent_web_files.csv` | Web 根目录、日志候选、近期文件和高风险扩展名 |

其中“所有用户”按平台可见数据源实现：Linux 默认读取 `/etc/passwd`（包含系统账户）并在可用时读取 `/etc/shadow`；Windows 枚举本地用户、域用户和 Administrators 成员，并额外尝试读取 SAM。SAM、shadow 或部分进程字段需要 root/管理员权限；读不到时会写入 `collection/collection_errors.csv` 和 `findings/evidence_gaps.csv`，空文件不表示“没有账户”或“没有问题”。

按时间定位新出现或被修改的工具：

```bash
./dumpall scan --updatetime --since 2026-08-23T00:00:00+08:00 --until 2026-08-26T23:59:59+08:00 --output ./updated-files
```

`--updatetime` 只读取文件元数据，不读取文件内容。结果写入 `findings/updated_files.csv`，并对 PsExec、Mimikatz、ProcDump、Rclone、Netcat、PowerShell、Certutil 等名称给出 `tool_hint`，名称提示不是恶意结论。

快速扫描不会自动打开主机 EVTX、audit/journald 和扩展主机痕迹。需要这些内容时使用 `triage` 或指定对应输入参数。

两种命令的边界如下：

| 命令 | 始终执行 | 默认额外执行 | 默认不执行 |
|---|---|---|---|
| `scan`（默认 `quick`） | 基础快照 + 快速规则检测/报告 | Web 日志发现与解析、近期文件线索 | 主机事件全量、shell/SSH/sudo 历史、注册表/WMI 深挖、静态/YARA、容器、原始证据副本、evidence pack、内存 |
| `triage` | 同一基础快照 + 完整规则/关联分析 | 主机事件、扩展痕迹、静态/运行时/容器检查、raw 原始副本、发现项证据副本、时间线、HTML/Markdown/XLSX、evidence pack | 内存（除非显式 `--memory-triage`、`--memory-dump` 或 `--memory-tool`） |

因此你的理解基本正确，但“triage 所有信息”是**有上限、可审计的完整应急采集面**，不是无限制磁盘镜像：原始副本受文件数、单文件大小、总容量和目录深度上限约束；权限不足、工具缺失和超限都会记录证据缺口。`scan --profile triage` 可以启用 triage 采集/分析档位，但若要复制 raw，请同时指定 `--copy-raw`；最直接的全量入口仍是 `triage` 命令。

### 2.2 应急采集（推荐）

Linux：

```bash
sudo ./dumpall triage --output /secure/dumpall-out
```

Windows：

```powershell
dumpall.exe triage -o D:\dumpall-out
```

`triage` 自动启用主机事件、主机扩展、原始证据复制、报告、时间线、静态扫描、容器节点侧检查和 evidence pack。默认**不读取或下载内存**。

### 2.3 低影响内存取证

```bash
sudo ./dumpall triage --memory-triage --output /secure/dumpall-memory
```

```powershell
dumpall.exe triage --memory-triage -o D:\dumpall-memory
```

该模式不暂停进程、不 attach、不读取整机物理内存，适合先在生产主机上做低影响筛查。

### 2.4 全量/外部内存采集

Linux 原生路线：

```bash
sudo ./dumpall triage --memory-dump --output /secure/dumpall-ram
```

外部工具路线：

```bash
sudo ./dumpall triage \
  --memory-tool /opt/avml \
  --output /secure/dumpall-ram
```

Windows 原生路线是逐进程 MiniDump，不是物理 RAM 镜像。物理内存镜像应使用经过审核的 WinPmem、DumpIt、AVML、LiME 等工具，并写入独立磁盘。

完整内存文件不会自动进入 ZIP evidence pack，而是留在 `raw/` sidecar，避免压缩过程扩大磁盘和内存压力。

## 3. 输出格式和性能

### 3.1 JSONL/CSV 是否负担过大

通常不会。JSONL 和 CSV 都是纯文本格式：

- JSONL 一条记录一行，适合逐行读取、追加、管道处理和保留嵌套字段。
- CSV 字段更紧凑，适合 Excel、脚本和数据分析工具。
- 当前写入器按记录写入 JSONL，CSV 使用 CSV writer；不会因为文件扩展名是 `.jsonl` 或 `.csv` 而把整个输出文件再复制一份。
- 真正的内存负担主要来自上游解析器保留的事件/发现项集合，不是 `.jsonl`/`.csv` 后缀。

把文件改名为 `.txt` 不会减少内容大小，也不会降低写入成本；如果只是把 CSV/JSONL 改成 TXT，数据展示形式可以保持不变，但下游工具会失去明确的类型识别，且容易破坏 dumpall 自己的复核、检测和 evidence pack 流程。

因此保留以下约定：

- `collection/*.csv`：主机采集表。
- `events/*.jsonl`：Windows/Linux 事件和应用日志事件。
- `findings/*.jsonl`、`findings/*.csv`：规则发现和证据缺口。
- `reports/*.md`、`reports/*.html`：人工阅读。
- `reports/dumpall_report.xlsx`：启用 `--xlsx-report` 或 `triage` 时生成。

`--format jsonl,csv,md,html` 当前用于记录报告格式元数据；核心 `collection`、`events` 和 `findings` 文件仍按上述固定扩展名生成，不是一个把所有文件切换成 TXT 的开关。

### 3.2 降低主机负担的参数

```bash
./dumpall triage \
  --log-days 7 \
  --max-event-records 50000 \
  --max-file-size 256 \
  --threads 2 \
  --no-xlsx-report \
  --output ./results
```

含义：

- `--log-days`：事件默认窗口，默认 30 天。
- `--max-event-records`：事件解析的记录上限，默认 200000；上限只消耗时间窗口内的记录（窗口外的旧记录不占用预算，最新时段证据优先保留）。
- `--max-file-size`：普通文件读取上限，默认 512 MB；二进制 EVTX 使用单独的更高下限。
- `--threads`：约束 EVTX 解析并发。
- `--no-xlsx-report`：不生成合并 Excel，减少额外写入。
- `--max-cpu`：当前记录为运行预算元数据，不等同于操作系统 cgroup/job object 限制。

原始 triage 副本有单文件 256 MB、最多 10000 文件、总量 2 GiB 的限制；超限会记录跳过信息。`export logs/tasks` 使用更保守的单文件上限 64 MB。

## 4. 命令和档位

### 4.1 `scan`

```bash
./dumpall scan --profile full-ir -w /var/www -l /var/log/nginx/access.log
```

适合完整规则检测和报告。`--profile` 常用值：

| 档位 | 主要内容 |
|---|---|
| `quick` | 基础主机、Web 输入和轻量检测 |
| `web` | Web/WAF/应用日志关联 |
| `web-db` | Web、WAF、应用和数据库日志 |
| `full-ir` | 主机扩展、静态扫描、日志关联、时间线和检测 |
| `host-ir` | 主机事件、时间线和主机扩展 |
| `runtime` | Java/Tomcat/Spring/IIS/ASP.NET 组件静态检查 |
| `container-ir` | 容器节点侧元数据和日志 |
| `triage` | 上述应急采集面、raw 副本和 evidence pack |

只打印计划、不采集：

```bash
./dumpall scan --dry-run-plan --profile triage --memory-triage
```

### 4.2 `collect`

```bash
sudo ./dumpall collect --output ./collect-out
```

执行基础主机采集：系统、进程、网络、账户、持久化和文件清单。默认 `quick` 档不会自动采集完整主机事件和扩展痕迹；可通过 `--profile host-ir` 或显式事件路径启用。

### 4.3 `analyze`

```bash
./dumpall analyze \
  --log-path ./access.log \
  --app-log-path ./application.log \
  --waf-log-path ./waf.log \
  --db-log-path ./mysql-error.log \
  --full-scan \
  --output ./analyze-out
```

只分析用户提供的离线输入，不自动读取本机主机状态。至少需要一个 `--log-path`、`--app-log-path`、`--waf-log-path`、`--db-log-path`、静态扫描路径、运行时输入、主机事件输入或容器输入。

### 4.4 `triage`

```bash
sudo ./dumpall triage \
  --redact \
  --memory-triage \
  --output ./triage-out
```

默认启用：

- Linux auth/audit/journald、Windows EVTX 目录。
- 进程、网络、账户、登录记录、计划任务、服务、启动项和持久化。
- SSH、sudo、shell history、内核模块、SUID、临时文件、删除文件、PAM、udev、LD_PRELOAD 等扩展。
- 原始日志和配置复制到 `raw/`，生成 SHA-256 清单。
- `evidence_pack/`、HTML/Markdown 报告和 triage 时间线。

`triage` 的证据副本分两层：

- `raw/`：固定的系统日志、配置、计划任务、事件文件等原始副本，带 `raw/raw_manifest.csv`。
- `evidence/suspicious_files/`：静态扫描/YARA 命中文件、可疑进程对应的可执行文件、发现项引用的源日志，以及显式传入的日志/事件目录中的文件；`evidence/evidence_copy_manifest.csv` 记录每个来源、目标、SHA-256、大小、修改时间、复制原因和状态。

复制受单文件 128 MiB、总量 1 GiB、最多 5000 个文件和目录深度 8 限制，跳过符号链接和结果目录。`--no-copy-raw` 会同时关闭 `raw/` 和上述发现项证据副本。凭据/私钥、完整内存和逐进程 dump 仍不会自动进入 ZIP evidence pack，而留在受限 sidecar。

`--redact` 只作用于解析结果和报告；原始 `raw/` 与证据副本保留原文，必须按敏感证据处理。

### 4.5 `export`

#### 进程

```bash
./dumpall export proc --redact --output ./proc-out
```

输出：

- `collection/processes.csv`
- `collection/process_tree.txt`
- Linux 额外输出 `collection/process_env.csv`

字段包括 PID、PPID、进程名、可执行路径、命令行、启动时间、用户和 Web 相关标记。Linux 启动时间目前保存为 `boot_ticks:<ticks>`，不是墙上时间；权限不足时命令行、exe 或用户可能为空，并会保留采集错误。

#### 网络

```bash
./dumpall export net --output ./net-out
```

输出：

- `collection/network_connections.csv`
- Linux：ARP、Unix socket、DNS/hosts/nsswitch、防火墙规则。
- Windows：DNS 缓存、hosts、SMB 共享、防火墙规则。

连接字段包括协议、本地/远端地址和端口、状态、PID、进程名和远端地址分类。Linux 通过 `/proc/net` 与 socket inode 关联进程；Windows 通过 `Get-NetTCPConnection`、`Get-NetUDPEndpoint` 与 CIM 进程映射。连接和进程是瞬时状态，采集期间发生变化属于正常现象。

#### 日志

```bash
./dumpall export logs \
  --log-days 7 \
  --since 2026-08-19T00:00:00+08:00 \
  --until 2026-08-26T23:59:59+08:00 \
  --output ./logs-out
```

输出：

- `raw/` 原始日志副本和 `raw/raw_manifest.csv`。
- `events/windows_events.jsonl`、`events/linux_events.jsonl`。
- `events/auth_events.csv`、`process_events.csv`、`service_events.csv`、`scheduled_task_events.csv`、`powershell_events.csv`。
- `events/event_parse_errors.csv` 和 `collection/collection_errors.csv`。

参数优先级：显式 `--since` 高于 `--log-days`；`--full-scan` 关闭事件时间窗口；`--until` 限制结束时间。Windows 二进制 EVTX 需要启用 `binary-evtx` 特性；文本/XML/JSON 事件文件超过 256 MB 会跳过并记录证据缺口，以防止应急工具自身耗尽内存。

#### 计划任务/启动项

```bash
./dumpall export tasks --redact --output ./tasks-out
```

输出计划任务、cron/systemd/启动项/服务以及对应 raw 文件。`--redact` 已传递到任务命令字段；原始副本仍不脱敏。

## 5. 路径参数说明

| 参数 | 生效范围 | 说明 |
|---|---|---|
| `--web-path/-w` | scan、full-ir、静态扫描 | Web 根目录，可重复指定 |
| `--log-path/-l` | scan/analyze/Web 日志 | 访问日志，可重复指定 |
| `--db-log-path` | `scan --profile web-db/full-ir`、`analyze` | 数据库日志；不会影响 `export` 单项 |
| `--app-log-path` | Web/Analyze | 应用框架日志 |
| `--waf-log-path` | Web/Analyze | WAF/CDN/代理日志 |
| `--evtx-path` | host-ir/triage/export logs | Windows EVTX/XML/JSON/JSONL |
| `--journal-path` | host-ir/triage/export logs | journald 导出文件/目录 |
| `--audit-log-path` | Linux host-ir/triage/export logs | auditd/auth/secure 日志 |
| `--updatetime` | scan/collect/triage | 系统范围 mtime 时间线；使用 `--since/--until` 或 `--time-range` 限制窗口 |
| `--runtime-scan` | runtime/full-ir/triage | 运行时组件静态排查，不 attach JVM |
| `--container-log-path` | container-ir/triage | 节点侧容器日志，不进入容器执行命令 |

`export proc`、`export net`、`export tasks` 不会因为传入 `--log-path`、`--db-log-path` 等参数而分析对应日志；这些参数因 CLI 复用而被接受，但对该单项不生效。`export logs` 主要使用事件路径和时间参数。

`export logs` 和 `export tasks` 的设计目标是“带走原始证据”，因此会自动复制对应 raw 文件；`--copy-raw`/`--no-copy-raw` 不改变这两个单项的固定行为。`--evidence-pack` 不会让 `export` 生成 evidence ZIP/TAR；如需证据包请使用 `triage`。`export` 会遵守 `--xlsx-report`/`--no-xlsx-report`，默认不生成 Excel。

## 6. 时间、脱敏和证据包

- `--time-range`：主要用于 Web/应用分析的最近小时窗口，默认 72 小时（最近 3 天）。
- `--log-days`：主机事件默认天数窗口，默认 30 天。
- `--since/--until`：显式时间边界；`--since` 优先于 `--log-days`。
- `--tz-offset`：无时区时间戳（多数数据库/应用/WAF 日志）的时区偏移，如 `+08:00` 或 `UTC`。默认取分析机本地时区；离线分析机与被检主机时区不一致时建议显式指定，否则跨日志源的时间对齐与关联窗口会整体偏移。带时区的时间戳（如 access.log 的 +0000、EVTX 的 UTC）不受影响。
- `--full-scan`：对指定日志和事件输入取消时间窗口过滤；与 `--updatetime` 一起使用时表示不限制文件修改时间范围，请确认磁盘和输出上限。
- 时间窗口的作用层次：采集侧按窗口过滤事件并整份带走原始日志；Web/数据库/应用日志的解析结果全量落盘（采集宽），规则检测阶段按窗口过滤（告警窄）；时间戳缺失的事件保守保留并单独计数。
- `--updatetime`：扫描系统文件修改时间，输出 `findings/updated_files.csv`；不会读取文件内容。
- `--redact`：脱敏 Cookie、Authorization、token、password、session、JWT 和连接串等派生字段。
- `--copy-raw`：复制关键原始日志/配置；`triage` 默认开启。
- `--no-copy-raw`：关闭 raw 副本，也会关闭 triage 的发现项证据副本，但不会关闭基础结构化采集。
- `--evidence-pack`：生成 ZIP/TAR 证据包；`triage` 默认开启。
- `--pack-format zip|tar`：选择证据包格式。

证据包包括报告、规则清单、发现项、错误、证据缺口、`findings/updated_files.csv` 和 triage 原始日志/配置；`evidence/suspicious_files/` 中的普通发现项源文件也会随包带走。密码哈希、私钥、注册表敏感 hive、完整内存和进程 dump 保留在受限 `raw/` sidecar，不自动进入 ZIP。

## 7. 内存取证操作建议

### 7.1 发现进程注入或内存马

先执行低影响路线：

```bash
sudo ./dumpall triage --memory-triage --redact --output ./triage-memory
```

优先检查：

1. `findings/memory_triage.csv` 中的 `writable_executable_mapping`、`anonymous_executable_mapping`、`deleted_or_temp_executable_mapping`。
2. Web 进程的匿名 heap/stack/RW 区域。
3. `raw/memory_triage/*.bin` 中的 URL、命令、Base64、脚本和凭据关键词。
4. `collection/processes.csv`、`collection/network_connections.csv`、登录和持久化记录的时间关联。

Windows 重点检查 `raw/memory_triage_processes/manifest.csv` 和生成的 `.dmp`，再在分析机使用 WinDbg、Volatility、PE-sieve 或 Moneta 做 VAD、模块和线程分析。

### 7.2 需要完整物理内存时

完整 RAM 采集不可避免地消耗磁盘、CPU 和内核资源。推荐：

- 先完成 `--memory-triage` 和普通 triage，确认目标和磁盘空间。
- 使用独立磁盘或网络块设备，避免写满系统盘。
- 只采集一次并记录工具版本、命令、开始/结束时间和 SHA-256。
- 将镜像带到分析机处理，不在受害主机上运行重型 Volatility 分析。

## 8. 补充应急脚本

发布包内 `scripts/ir/` 存放与平台分离的手动脚本。它们用于补充 `triage` 的深度证据，不会在主程序中自动执行，也不会把浏览器 Cookie、登录密码、云凭据内容或完整物理内存自动打包。

### 8.1 Linux

```bash
sudo ./scripts/ir/linux/run_all.sh --output /secure/ir/linux-extra
```

`01_volatile_context.sh` 固定先采集 `/proc`、socket、路由、systemd、journal/audit、eBPF、keyring、lsof 等易失现场；`02_filesystem_metadata.sh` 采集 bodyfile、ACL、不可变属性、未知 UID/GID、coredump、binfmt_misc；`03_application_artifacts.sh` 记录浏览器/客户端历史、远程管理/云配置元数据和容器状态。`--parallel` 只在至少 4 GiB RAM、4 核且负载低于核数时并发第 2、3 组。所有模块默认有 32 MiB 单输出上限和状态/哈希清单。

目标进程内存必须显式指定 PID：

```bash
sudo ./scripts/ir/linux/04_targeted_memory.sh --output /secure/ir/linux-memory --pid 1234
sudo ./scripts/ir/linux/04_targeted_memory.sh --output /secure/ir/linux-memory \
  --pid 1234 --capture-dump --gcore /trusted/usr/bin/gcore
```

第二条命令逐进程执行 `gcore`，有 300 秒超时和可用空间检查；它只生成目标进程 dump，不是物理内存镜像。`run_all.sh` 不会自动调用该脚本。

### 8.2 Windows

管理员 PowerShell 5.1+：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\ir\windows\Run-All.ps1 -Output D:\ir\windows-extra
```

Windows 脚本覆盖进程 owner、模块/线程、命名管道、SMB、WMI/BITS/Defender、防火墙、事件配置、USN Journal 元数据、Prefetch/Amcache/SRUM/PCA/WER、离线用户 hive、Jump List/LNK/RDP/ActivitiesCache、浏览器与 PowerShell history。需要受限 USN 记录时给 `Run-All.ps1` 增加 `-CollectUsn`（最多 50000 行）；复制受单文件 256 MiB、总量预算和文件数限制；被锁文件会尝试 `esentutl` 只读复制，仍失败则写入清单。

目标 PID 内存：

```powershell
.\scripts\ir\windows\04-TargetedMemory.ps1 -Output D:\ir\memory -ProcessId 1234,5678
.\scripts\ir\windows\04-TargetedMemory.ps1 -Output D:\ir\memory `
  -ProcessId 1234 -CaptureDump -ProcDumpPath D:\tools\procdump64.exe
```

默认只采集进程、模块、线程；`-CaptureDump` 要求 ProcDump 路径，默认校验 Authenticode 签名，逐 PID 串行运行，5 分钟超时且空间不足自动跳过。`Run-All.ps1 -Parallel` 仅在至少 4 GiB RAM、4 逻辑核且 CPU 低于 70% 时并发文件/应用模块；易失信息先串行，内存永不自动采集。

脚本输出的 `status.tsv`、`evidence_copy_manifest.tsv`、`SHA256SUMS.txt` 必须与主程序的 `collection_errors.csv`、`evidence_gaps.csv` 一并交给分析人员。完整边界、外置工具和不能由低影响 triage 证明的项目见 [`docs/TRIAGE_COVERAGE.md`](docs/TRIAGE_COVERAGE.md) 与 [`scripts/ir/README.md`](scripts/ir/README.md)。

## 9. 常见问题

### 输出目录已存在

换一个目录名；工具拒绝覆盖已有目录，以避免证据混写。

### 进程命令行或网络 PID 为空

检查是否以 root/管理员运行。Linux `/proc`、Windows CIM 和受保护进程都可能拒绝读取。查看 `collection/collection_errors.csv`，空字段不代表“没有进程参数”。

### Windows 事件为空

确认以管理员运行，并检查 `--evtx-path` 是否指向 `C:\Windows\System32\winevt\Logs`。二进制 `.evtx` 使用带 `binary-evtx` 特性的构建；权限、路径或事件文件过大时查看 `events/event_parse_errors.csv` 和 `collection/collection_errors.csv`。

### JSONL/CSV 想改成 TXT

不建议直接改扩展名。TXT 不会降低负载；保留 CSV/JSONL 作为机器可读证据，使用 `reports/*.html`、`reports/*.md` 或 Excel 作为展示层。

## 10. 说明

Windows 对应 amd64、x86（32 位）和 ARM64；——只适用于Windows

Linux 对应 amd64、x86（32 位）、ARM64 和 ARM32。——只适用于Linux

不同系统和架构之间不能混用二进制。——需要其他的自行编译吧。
